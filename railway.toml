[build]
builder = "DOCKERFILE"

[deploy]
watchPatterns = [
  "app/**/*.py",
  "app/templates/**",
  "app/static/css/**",
  "app/static/js/**",
  "requirements.txt",
  "Dockerfile",
  "wsgi.py",
  "entrypoint.sh",
  "railway.toml",
]